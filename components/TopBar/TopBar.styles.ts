import { createStyles } from '@mantine/core';

const useStyles = createStyles(() => ({
  buttonContainer: {
    margin: 'auto',
    width: '100%',
    padding: '0 1.3125rem 0 1.3125rem',
  },
}));

export { useStyles };
